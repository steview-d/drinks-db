$(document).ready(function() {
    // Materialize SideNav
    $('.sidenav').sidenav();
    
    // Back button
    $('.history-back').click(function() {
        window.history.back();
    });

    //

});
